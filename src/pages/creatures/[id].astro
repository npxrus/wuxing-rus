---
// Динамическая страница для отдельного существа
import BaseLayout from '@layouts/BaseLayout.astro';
import ElementBadge from '@components/ElementBadge.astro';
import { DANGER_LEVELS } from '@data/dangerLevels';
import { ALL_CREATURES } from '@data/creatures';
import { withBase } from '@utils/paths';

export async function getStaticPaths() {
  return ALL_CREATURES.map(creature => ({
    params: { id: creature.id },
    props: { creature },
  }));
}

const { creature } = Astro.props;
const { 
  name, 
  category, 
  categoryName,
  element, 
  description, 
  appearance, 
  habitat, 
  abilities, 
  behavior, 
  interaction, 
  size, 
  dangerLevel,
  icon, 
  quote,
  traces,
  sounds,
  smell,
  tamable,
  tameMethod,
  tameReward
} = creature;

const dangerInfo = DANGER_LEVELS[dangerLevel];
const categoryLabel = categoryName;
---

<BaseLayout title={name}>
  <div class="page-header">
    <div class="header-icon">
      <span>{icon}</span>
    </div>
    <div class="header-text">
      <h1>{name}</h1>
      <div class="header-meta">
        <span class="category-badge">{categoryLabel}</span>
        {element && <ElementBadge element={element} size="medium" />}
        <span class="danger-badge" style={`background: ${dangerInfo.color}20; color: ${dangerInfo.color}; border-color: ${dangerInfo.color}`}>
          {dangerInfo.icon} {dangerInfo.name}
        </span>
        {tamable && <span class="tamable-badge">🐕 Можно приручить</span>}
      </div>
    </div>
  </div>

  {quote && (
    <div class="quote-block">
      <p class="quote-text">«{quote}»</p>
    </div>
  )}

  <section class="section">
    <h2>📜 Описание</h2>
    <p class="description-text">{description}</p>
  </section>

  <section class="section two-columns">
    <div class="column">
      <h3>👁️ Внешний облик</h3>
      <div class="info-card">
        {size && <div class="size-badge">Размер: {size}</div>}
        <p>{appearance}</p>
      </div>
    </div>
    <div class="column">
      <h3>🏞️ Обитание</h3>
      <div class="info-card">
        <p>{habitat}</p>
      </div>
    </div>
  </section>

  <section class="section">
    <h2>✨ Способности</h2>
    <div class="abilities-list">
      {abilities.map(ability => (
        <div class="ability-card">
          <span class="ability-icon">⚡</span>
          <span>{ability}</span>
        </div>
      ))}
    </div>
  </section>

  {traces && (
    <section class="section">
      <h2>👣 Следы и признаки</h2>
      <div class="info-card">
        <p><strong>Следы:</strong> {traces}</p>
        {sounds && <p><strong>Звуки:</strong> {sounds}</p>}
        {smell && <p><strong>Запах:</strong> {smell}</p>}
      </div>
    </section>
  )}

  <section class="section">
    <h2>🎭 Поведение</h2>
    <div class="behavior-card">
      <p>{behavior}</p>
    </div>
  </section>

  <section class="section">
    <h2>🤝 Взаимодействие с человеком</h2>
    <div class="interaction-card">
      <p>{interaction}</p>
    </div>
  </section>

  {tamable && tameMethod && tameReward && (
    <section class="section">
      <h2>🐕 Приручение</h2>
      <div class="tame-card">
        <p><strong>Способ:</strong> {tameMethod}</p>
        <p><strong>Награда:</strong> {tameReward}</p>
      </div>
    </section>
  )}

  <div class="back-link">
    <a href={withBase('/creatures')}>← Вернуться к списку существ</a>
  </div>
</BaseLayout>

<style>
  .page-header {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    margin-bottom: 2rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--border-color);
    flex-wrap: wrap;
  }
  
  .header-icon {
    font-size: 4rem;
    background: var(--bg-card);
    width: 100px;
    height: 100px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 2px solid var(--border-color);
  }
  
  .header-text h1 {
    margin: 0 0 0.5rem 0;
  }
  
  .header-meta {
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
  }
  
  .category-badge {
    padding: 0.3rem 0.8rem;
    border-radius: 20px;
    font-size: 0.8rem;
    background: var(--bg-secondary);
    color: var(--text-muted);
  }
  
  .danger-badge {
    padding: 0.3rem 0.8rem;
    border-radius: 20px;
    font-size: 0.8rem;
    border: 1px solid;
  }
  
  .tamable-badge {
    padding: 0.3rem 0.8rem;
    border-radius: 20px;
    font-size: 0.8rem;
    background: rgba(34, 197, 94, 0.2);
    color: #22c55e;
  }
  
  .section {
    margin-bottom: 2.5rem;
  }
  
  .quote-block {
    background: linear-gradient(135deg, var(--bg-card) 0%, rgba(212, 175, 55, 0.05) 100%);
    border-left: 4px solid var(--text-accent);
    padding: 1.5rem;
    margin-bottom: 2rem;
    border-radius: var(--border-radius-md);
  }
  
  .quote-text {
    font-size: 1.1rem;
    font-style: italic;
    color: var(--text-primary);
    margin: 0;
  }
  
  .description-text {
    font-size: 1.05rem;
    line-height: 1.7;
    color: var(--text-secondary);
  }
  
  .two-columns {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }
  
  .info-card, .behavior-card, .interaction-card, .tame-card {
    background: var(--bg-card);
    border-radius: var(--border-radius-md);
    padding: 1.25rem;
    border: 1px solid var(--border-color);
  }
  
  .size-badge {
    display: inline-block;
    background: var(--text-accent);
    color: var(--bg-primary);
    padding: 0.2rem 0.6rem;
    border-radius: 20px;
    font-size: 0.7rem;
    margin-bottom: 0.75rem;
  }
  
  .abilities-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }
  
  .ability-card {
    background: var(--bg-card);
    border-radius: 30px;
    padding: 0.5rem 1rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    border: 1px solid var(--border-color);
    transition: all var(--transition-fast);
  }
  
  .ability-card:hover {
    border-color: var(--text-accent);
    transform: translateY(-2px);
  }
  
  .ability-icon {
    font-size: 1rem;
  }
  
  .back-link {
    margin-top: 3rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border-color);
    text-align: center;
  }
  
  .back-link a {
    color: var(--text-accent);
    text-decoration: none;
  }
  
  .back-link a:hover {
    text-decoration: underline;
  }
  
  @media (max-width: 768px) {
    .page-header {
      flex-direction: column;
      text-align: center;
    }
    
    .header-meta {
      justify-content: center;
    }
    
    .two-columns {
      grid-template-columns: 1fr;
    }
    
    .header-icon {
      width: 70px;
      height: 70px;
      font-size: 2.5rem;
    }
  }
</style>